SkyCube asset folder

Optional texture files currently recognized by models.js:
- plane.png
- propeller.png
- tree.png

The game has procedural/vertex-color fallbacks, so these files are not required.
Add your textures here later without changing index.html.
